pr: none
trigger: none

pool: Default

resources:
  webhooks:
    - webhook: case_sangeethaa          ### Webhook alias
      connection: case_sangeethaa    

steps:
- ${{ each value in parameters.secureFilesWebhook.listOfSecureFiles }}:
  - task: DownloadSecureFile@1
    inputs:
      secureFile: ${{ value }}